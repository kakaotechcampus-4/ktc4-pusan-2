# 복사해서 .env.local로 쓰세요. .env.local은 커밋되지 않습니다.
#   cp .env.example .env.local

# 목 서버를 끄고 실서버로 붙일 때 (W4 목요일)
VITE_USE_MOCK=true

# 실서버 주소. VITE_USE_MOCK=false일 때만 씁니다.
VITE_API_BASE=http://localhost:8000
